{
  "name": "@sightline/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "sightline": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@sightline/core": "workspace:*",
    "@sightline/db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}

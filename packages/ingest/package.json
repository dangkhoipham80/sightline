{
  "name": "@sightline/ingest",
  "version": "0.0.0",
  "private": true,
  "description": "Discovers, groups and indexes Claude Code transcripts.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@sightline/core": "workspace:*",
    "@sightline/db": "workspace:*",
    "chokidar": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
